height = float(input("Enter the height of Triangle : "))   # Taking height from user
base = float(input("Enter the base of Triangle : "))       # Taking base from user
Area = (height * base)/2                                   # Formula: Area = (height * base)/2
print("Area of Triangle : ",Area)                          # Display the result