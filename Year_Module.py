from datetime import datetime    # Importing the datetime class from the datetime module
year = datetime.now().year       # Getting the current date and time using now() and extracting only the current year from it
print(year)                      # Printing the current year   